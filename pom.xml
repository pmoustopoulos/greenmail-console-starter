<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>io.github.pmoustopoulos</groupId>
    <artifactId>greenmail-console-spring-boot-starter</artifactId>
    <version>0.1.1</version>
    <name>greenmail-console-spring-boot-starter</name>
    <description>In-memory GreenMail SMTP server with a web console, as a Spring Boot starter</description>
    <url>https://github.com/pmoustopoulos/greenmail-console-starter</url>

    <licenses>
        <license>
            <name>The Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>pmoustopoulos</id>
            <name>Petros Moustopoulos</name>
            <url>https://github.com/pmoustopoulos</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/pmoustopoulos/greenmail-console-starter.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/pmoustopoulos/greenmail-console-starter.git</developerConnection>
        <url>https://github.com/pmoustopoulos/greenmail-console-starter</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <java.version>21</java.version>
        <greenmail.version>2.1.6</greenmail.version>
        <swagger-annotations-jakarta.version>2.2.53</swagger-annotations-jakarta.version>
    </properties>

    <dependencies>
        <!-- Auto-configuration machinery -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- Web layer: controller + JSON. Optional so it never forces web onto a consumer that lacks it,
             but present for this module's own compilation and tests. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Security: only needed for the conditional security auto-config. Optional. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- JSON for the console API. The console uses its own private JsonMapper (never the host's
             bean), so a customised host mapper cannot change the wire format. Version managed by Boot. -->
        <dependency>
            <groupId>tools.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- The in-memory mail server (compile scope: it is the core value of this starter) -->
        <dependency>
            <groupId>com.icegreen</groupId>
            <artifactId>greenmail</artifactId>
            <version>${greenmail.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
            <version>${swagger-annotations-jakarta.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Spring's mail support, TEST scope only. The starter itself never provides a JavaMailSender:
             consumers point their own spring.mail.* at the embedded SMTP server (like H2's JDBC URL), so
             swapping to a real mail server is just standard Spring Boot mail config with nothing to undo.
             This dependency lets the starter's own integration test act as a consumer — sending mail into
             the embedded server and asserting the console captures it. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <!-- Targets Java 21 bytecode regardless of the JDK used to build -->
                    <release>${java.version}</release>
                </configuration>
            </plugin>

            <!-- Required by Maven Central: attach the sources JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Required by Maven Central: attach the Javadoc JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <!-- Don't fail the release over missing/incomplete Javadoc comments -->
                    <doclint>none</doclint>
                    <quiet>true</quiet>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Activate with `mvn clean deploy -Prelease` to sign and publish to Maven Central. -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <!-- Required by Maven Central: GPG-sign every artifact -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <!-- Non-interactive signing in CI (no TTY for a passphrase prompt).
                                         The passphrase is supplied via the MAVEN_GPG_PASSPHRASE env var. -->
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- Uploads the signed bundle to the Sonatype Central Portal -->
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.9.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <!-- Must match the <server><id> in your ~/.m2/settings.xml -->
                            <publishingServerId>central</publishingServerId>
                            <!-- Set to true once you're confident, to skip the manual "Publish" click -->
                            <autoPublish>false</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
